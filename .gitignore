# Large firmware images (OpenWrt, backups)
# *.bin  # Commented out to allow U-Boot and ART files

# Keep images in the images folder
!images/*.jpg
!images/*.png

# Keep binary files in binaries folder
!binaries/*.bin

# Private keys (never commit!)
*.key
*private*

# Backups
*.bak
*.backup

# OS files
.DS_Store
Thumbs.db

# Editor files
*.swp
*.swo
*~
